# Copy this file to .env.local (development) or .env.production (production).

# URL of the Django API — no trailing slash
NEXT_PUBLIC_API_URL=https://crm.solarhopeafrica.com/api

# Used in next.config.js to allow product images from the API server
API_HOSTNAME=crm.solarhopeafrica.com
